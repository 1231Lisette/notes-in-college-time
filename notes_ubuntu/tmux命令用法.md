# 常用用法

## ① 会话（session）相关 —— **最重要**

```bash
tmux new -s 名字          # 新建一个会话（一个实验）
tmux ls                  # 列出所有会话
tmux attach -t 名字       # 回到某个会话
tmux kill-session -t 名字 # 结束某个会话（慎用）
tmux rename-session -t 旧名 新名
```

------

## ② tmux 快捷键（只要记住这几个）

> 所有快捷键都是：**先 Ctrl + B，再按一个键**

| 操作       | 按键              |
| ---------- | ----------------- |
| 离开但不关 | `Ctrl+B → D`      |
| 新窗口     | `Ctrl+B → C`      |
| 切换窗口   | `Ctrl+B → 数字`   |
| 左右分屏   | `Ctrl+B → %`      |
| 上下分屏   | `Ctrl+B → "`      |
| 会话列表   | `Ctrl+B → S`      |
| pane 切换  | `Ctrl+B → 方向键` |

------

# 二、训练 & GPU 监控（你天天要看）

```bash
nvidia-smi                 # 看 GPU 占用
watch -n 1 nvidia-smi      # 实时刷新（最常用）
htop                       # 看 CPU / 内存
```

------

# 三、进程管理（排错必用）

```bash
ps aux | grep python       # 找训练进程
ps aux | grep train
kill PID                   # 杀进程
kill -9 PID                # 强制杀（最后手段）
```

------

# 四、SSH & 网络（你现在这个环境必会）

```bash
ssh user@服务器IP          # 登录服务器
exit                       # 退出 SSH
```

（**注意**：退出 SSH ≠ 训练停止，只要在 tmux 里）

------

# 五、conda（你的 Python 环境）

```bash
conda env list             # 看所有环境
conda activate lerobot     # 激活环境
conda deactivate           # 退出环境
```

------

# 六、文件 & 日志（训练一定会用）

```bash
ls                         # 列文件
ls -lh                     # 带大小
cd 路径                    # 切目录
pwd                        # 看当前路径

tail -f 文件.log           # 实时看日志（非常常用）
cat 文件                   # 看文件
less 文件                  # 翻页看文件
```

------

# 七、磁盘 & 空间（训练久了必查）

```bash
df -h                      # 看磁盘剩余
du -sh *                   # 看目录占用这些现在不是重点，以后再学：

iptables / nft

systemctl

docker

rsync

screen（tmux 的老版本）

你现在 tmux + conda + nvidia-smi 已经够你科研起飞了。

十、给你一句“服务器使用心法”
会话用 tmux，实验靠命名，状态看 nvidia-smi，日志别看终端。
```

------

# 八、最常见「组合用法」（你会天天这样用）

## 🔹 1️⃣ 起一个训练（标准流程）

```bash
ssh server
tmux new -s train-lerobot-bc-001

conda activate lerobot
python train.py
```

分屏：

```
Ctrl+B → %
```

右边：

```bash
watch -n 1 nvidia-smi
```

离开：

```
Ctrl+B → D
```

------

## 🔹 2️⃣ 第二天回来继续看

```bash
ssh server
tmux ls
tmux attach -t train-lerobot-bc-001
```

------

## 🔹 3️⃣ 看日志但不打扰训练

```bash
tail -f outputs/train/log.txt
```

------



